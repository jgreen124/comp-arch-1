# Parallel Square Root Computation using Newton's method

Problem 2 Implementation

## Build

The `build.sh` script requires `ispc` and `g++`

```bash
chmod +x ./build.sh
./build.sh
```

## Run

```bash
chmod +x ./run.sh
./run.sh
```

## Plots

```bash
python3 plot_perf.py
```